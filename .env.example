# Copy to .env.local for local dev. In production, set these in Vercel project
# settings → Environment Variables. If both are unset, the site stays open
# (useful for first deploy / debugging).
AUTH_USERNAME=
AUTH_PASSWORD=
